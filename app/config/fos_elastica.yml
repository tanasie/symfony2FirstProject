fos_elastica:
    clients:
        default: { host: %elastic_host%, port: %elastic_port% }
    indexes:
        acme_elastica:
            client: default
            types:
                article:
                    mappings:
                        id:
                            type: integer
                        createdAt :
                            type : date
                        publishedAt :
                            type : date
                        published :
                            type : boolean
                        title : ~
                        content : ~
                    persistence:
                        driver: orm
                        model: Acme\ElasticaBundle\Entity\Article
                        finder: ~
                        provider: ~
                        listener: ~